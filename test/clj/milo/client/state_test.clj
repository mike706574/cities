(ns milo.client.state-test
  (:require [clojure.test :refer [deftest testing is]]
            [milo.client.state :as state]))

(def initial {:socket socket
              :player player
              :screen :splash
              :events #{}
              :status-message "Loading page..."})

(deftest initial-state
  (is (=








       ))













  )
